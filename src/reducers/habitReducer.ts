// TODO: provide habitReducer data model
// TODO: assign action types

const initialState = {
  errorMessage: '',
};

function habitReducer(state = initialState, action: any) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}

export default habitReducer;
